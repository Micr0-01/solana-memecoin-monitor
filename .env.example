# Solana Memecoin Monitor Environment Configuration

# === REQUIRED SETTINGS ===

# Telegram Bot Configuration (REQUIRED)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Solana RPC Endpoints (REQUIRED)
SOLANA_RPC_HTTP=https://api.mainnet-beta.solana.com
SOLANA_RPC_WS=wss://api.mainnet-beta.solana.com

# === ALERT THRESHOLDS ===

# Volume threshold in USD (default: 50000)
VOLUME_THRESHOLD_USD=50000

# Liquidity threshold in USD (default: 2000)  
LIQUIDITY_THRESHOLD_USD=2000

# Alert cooldown in seconds (default: 3600 = 1 hour)
ALERT_COOLDOWN_SECONDS=3600

# Monitor mode: "since_first_trade" or "24h" (default: since_first_trade)
MONITOR_MODE=since_first_trade

# === OPTIONAL API KEYS ===

# Pump.fun Portal API Key (optional, for enhanced token data)
PUMPPORTAL_KEY=your_pump_portal_key_here

# GMGN API Key (optional, for additional token information)
GMGN_KEY=your_gmgn_key_here

# === DATABASE SETTINGS ===

# Redis URL for persistent storage (optional, defaults to local Redis)
REDIS_URL=redis://localhost:6379

# === TESTING AND DEVELOPMENT ===

# Enable mock mode for testing (true/false)
MOCK_MODE=false

# Enable dry run mode (no actual alerts sent) (true/false)
DRY_RUN=false

# Custom config file path (optional)
CONFIG_PATH=config/config.json

# === PERFORMANCE SETTINGS ===

# Maximum tokens to track simultaneously (default: 10000)
MAX_TOKENS_TRACKED=10000

# Price update interval in milliseconds (default: 30000 = 30 seconds)
PRICE_UPDATE_INTERVAL_MS=30000

# === LOGGING ===

# Log level: error, warn, info, debug (default: info)
LOG_LEVEL=info

# Enable file logging (true/false)
LOG_TO_FILE=true